# DFS-CIA-EA-CV-CYP-GCA v1 production campaign

This campaign tests a directional recurrent-flux signature derived from every
Graph-CA state transition. It separately accumulates positive and negative
channel-wise cellular-state changes across the recurrent horizon, retaining
the direction of molecular state movement that the existing squared transition
energy discards. Both accumulators remain differentiable through every
generation and enter the closed-form ridge readout as trajectory features.

The bounded screen compares the established fingerprint with and without the
directional-flux signature for every selected endpoint-rule candidate across
two scaffold folds. Selected candidates then undergo the established five-fold,
two-seed confirmation and sealed-holdout protocol.

Atoms remain chemically encoded cellular-automaton cells, typed bonds govern
local graph interactions, and nonlinear recurrent rules produce explicit
successive generations. The campaign retains trajectories, backpropagates
through every recurrent step and the genuine differentiable ridge solve, uses
scaffold-safe selection, keeps the sealed holdout isolated, and excludes blind
labels. USR-CIA-EA-CV-CYP-GCA remains the sealed-validation leader at point
MA-ST-RAE 0.748039 and RMSE 0.845606 pIC50.
